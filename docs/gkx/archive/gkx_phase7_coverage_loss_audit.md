# GKX Phase 7 coverage-loss audit

Window for panel/datashare comparison: `signal_yyyymm` **201801**–**202312**.

Sample Compustat build: `STOCK_CHARACTERS_SAMPLE_START=2018-01-01`, `END=2023-12-31`.

Generated by `scripts/audit_gkx_phase7_coverage_loss.py` (WRDS trace + on-disk panel counts).

## Critical finding: annual vs monthly counting

Datashare non-null counts are **monthly** (June-expanded) observations.
Repository `outputs/characteristics/individual/*.csv` row counts are **annual fiscal** rows.
Compare stage **6_monthly_expansion** to datashare — **not** raw annual CSV rows.

## Green SAS vs repository — side by side

### `chato` / `chatoia`

**Green SAS** (`Supplementary_assistive_files/SAS_codes/Greens_code.sas`):
```sas
chpm=(ib/sale)-(lag(ib)/lag(sale));
chato=(sale/((at+lag(at))/2)) - (lag(sale)/((lag(at)+lag2(at))/2));
...
array req{*} ... chpm ... pchcapx ...;
if count=1 then do;
  do b=1 to dim(req);
    req(b)=.;
  end;
end;
if count<3 then do;
  chato=.;
  grcapx=.;
end;
...
proc sql;
  create table data2 as select *,
    chpm-mean(chpm) as chpmia,
    chato-mean(chato) as chatoia,
    pchcapx-mean(pchcapx) as pchcapx_ia,
    ...
  from data2
  group by sic2,fyear;
```

**Repository** (`Character_Builders/_shared/green_builders.py`):
```python
comp['chpm'] = safe_divide(comp['ib'], comp['sale']) - safe_divide(comp['lag_ib'], comp['lag_sale'])
comp['chato'] = safe_divide(comp['sale'], avg_at) - safe_divide(
    comp['lag_sale'], (comp['lag_at'] + comp['lag2_at']) / 2)
grouped = comp.groupby(['fyear', 'sic2'], dropna=False)
comp['chatoia'] = comp['chato'] - grouped['chato'].transform('mean')
comp['chpmia'] = comp['chpm'] - grouped['chpm'].transform('mean')
comp.loc[comp.groupby('gvkey').cumcount() < 2, ['chato', 'chatoia']] = np.nan
comp.loc[comp.groupby('gvkey').cumcount() == 0, [..., 'chpmia', 'pchcapx_ia']] = np.nan
```

#### `chatoia` algebra verification

| Component | Green | Repository | Match? |
| --- | --- | --- | --- |
| Level term | `sale / ((at+lag(at))/2)` | `sale / avg_at` where `avg_at=(at+lag_at)/2` | Yes |
| Lag term | `lag(sale) / ((lag(at)+lag2(at))/2)` | `lag_sale / ((lag_at+lag2_at)/2)` | Yes |
| Difference | level − lag | same | Yes |
| Industry adj | `chato - mean(chato)` by `sic2,fyear` | `chato - group_mean` by `sic2,fyear` | Yes |
| SIC2 | `substr(sic,1,2)` | `sic // 100` | Yes |
| History rule | `count<3` → missing | `cumcount<2` → NaN | Yes (0-based vs 1-based) |
| Demean order | after row nulling | before final row nulling | Ordering differs; masked rows NaN in both |

### `chpm` / `chpmia`

**Green SAS:** `chpm=(ib/sale)-(lag(ib)/lag(sale));` → `chpmia=chpm-mean(chpm)` by `sic2,fyear`;
`chpm` in `req` array → missing when `count=1`.

**Repository:** same base ratio; `chpmia = chpm - group_mean(chpm)`; mask when `cumcount==0`.

### `pchcapx` / `pchcapx_ia`

**Green SAS:**
```sas
if missing(capx) and count>=2 then capx=ppent-lag(ppent);
pchcapx=(capx-lag(capx))/lag(capx);
pchcapx_ia=pchcapx-mean(pchcapx);  /* sic2,fyear */
```

**Repository:**
```python
impute_capx = comp['capx'].isna() & (firm_count >= 1)  # firm_count>=1 ≡ Green count>=2
comp.loc[impute_capx, 'capx'] = comp['ppent'] - comp['lag_ppent']
comp['pchcapx'] = safe_divide(comp['capx'] - comp['lag_capx'], comp['lag_capx'])
comp['pchcapx_ia'] = comp['pchcapx'] - grouped['pchcapx'].transform('mean')
```

---

## `chatoia` pipeline trace

| Stage | Rows | Non-null | Notes |
| --- | ---: | ---: | --- |
| 1_raw_compustat | 43,718 | 43,718 | WRDS `comp.funda` after `ANNUAL_COMPUSTAT_WHERE` + sample date filter |
| 2_base_characteristic | 43,718 | 25,416 | Formula applied; lags created; **before** Green history mask |
| 3_lag_history_mask | 43,718 | 25,416 | After Green `count` rules on base variable |
| 4_industry_adjustment | 43,718 | 25,416 | After SIC2×fyear mean demean; before final ia row mask |
| 5_annual_output_ccm | 30,851 | 18,126 | After CCM inner join (permno attach) |
| 5b_annual_output_written | 18,126 | 18,126 | Non-null only (same as `write_character`) |
| 6_monthly_expansion | 136,133 | 136,133 | June convention; dedupe permno×signal_yyyymm |
| 7_signal_panel | 392,674 | 108,375 | On-disk signal panel merge |
| 8_complete_panel | 278,522 | 74,741 | On-disk complete panel merge |
| on_disk_annual_csv | 14,184 | 14,184 | `outputs/characteristics/individual/chatoia.csv` |
| datashare.csv (monthly, window) | 286,695 | 190,718 | Full-history GKX benchmark |
| production_check (5b recompute) | 18,126 | 18,126 | Must match 5b |

### Where observations are lost

| Transition | Rows lost (non-null) | Expected? | Green parity? | Repo more restrictive? |
| --- | ---: | --- | --- | --- |
| 1→2 base formula / divide-by-zero | 18,302 | Partial (missing inputs) | Yes | No |
| 2→3 lag-history mask | 0 | **Yes** — mask redundant here; missing `lag2_at` already NaN in 2018–2023 window | Yes | No |
| 3→4 industry demean | 0 | No (same non-null) | Yes | No |
| 4→5 CCM inner join | 7,290 | **Yes** (no CRSP link) | Yes | No |
| 5→5b NaN/inf drop | 0 | **Yes** (`write_character`) | Yes | No |
| 5b→6 monthly expansion | non-null 18,126 → 136,133 | **Expected** (×~12 months) | Yes | No |
| 6→7 signal panel | 27,758 | Merge/filter | N/A | Only if panel build differs |
| 7→8 complete panel | 33,634 | Merge/filter | N/A | Only if panel build differs |
| 6 vs datashare (monthly) | repo 136,133 vs DS 190,718 | **Sample + history** | Green full history | Truncated sample, not formula |

### Assessment

- **Largest loss in this sample:** stage 1→2 (18,302 non-null dropped). Missing `sale`, `at`, or **insufficient lag history** (`lag_at`, `lag2_at`, `lag_sale`) within the 2018–2023 pull already enforces Green `count<3`; stage 2→3 explicit mask removes **0** additional rows here.
- **CCM inner join** (stage 4→5) drops 7,290 further non-null rows.
- **Monthly vs datashare:** fresh WRDS recompute expands to **136,133** non-null monthly vs datashare **190,718** (−54,585). Remaining gap is **truncated Compustat history**, not formula or CCM alone.
- **On-disk CSV stale:** `chatoia.csv` has **14,184** rows (`datadate` 2019-06-30 – 2023-12-31) vs fresh WRDS **18,126** at stage 5b. The 14,184 figure in earlier validation is an **under-counted stale artifact**, not the current pipeline output.
- **Green parity:** algebra, SIC2×fyear demean, and lag structure match Green; repo is **not more restrictive**.

## `chpmia` pipeline trace

| Stage | Rows | Non-null | Notes |
| --- | ---: | ---: | --- |
| 1_raw_compustat | 43,718 | 43,718 | WRDS `comp.funda` after `ANNUAL_COMPUSTAT_WHERE` + sample date filter |
| 2_base_characteristic | 43,718 | 29,252 | Formula applied; lags created; **before** Green history mask |
| 3_lag_history_mask | 43,718 | 29,252 | After Green `count` rules on base variable |
| 4_industry_adjustment | 43,718 | 29,252 | After SIC2×fyear mean demean; before final ia row mask |
| 5_annual_output_ccm | 30,851 | 22,444 | After CCM inner join (permno attach) |
| 5b_annual_output_written | 22,444 | 22,444 | Non-null only (same as `write_character`) |
| 6_monthly_expansion | 189,034 | 189,034 | June convention; dedupe permno×signal_yyyymm |
| 7_signal_panel | 392,674 | 189,034 | On-disk signal panel merge |
| 8_complete_panel | 278,522 | 129,877 | On-disk complete panel merge |
| on_disk_annual_csv | 22,444 | 22,444 | `outputs/characteristics/individual/chpmia.csv` |
| datashare.csv (monthly, window) | 286,695 | 188,942 | Full-history GKX benchmark |
| production_check (5b recompute) | 22,444 | 22,444 | Must match 5b |

### Where observations are lost

| Transition | Rows lost (non-null) | Expected? | Green parity? | Repo more restrictive? |
| --- | ---: | --- | --- | --- |
| 1→2 base formula / divide-by-zero | 14,466 | Partial (missing inputs) | Yes | No |
| 2→3 lag-history mask | 0 | **Yes** (Green count rules) | Yes | No |
| 3→4 industry demean | 0 | No (same non-null) | Yes | No |
| 4→5 CCM inner join | 6,808 | **Yes** (no CRSP link) | Yes | No |
| 5→5b NaN/inf drop | 0 | **Yes** (`write_character`) | Yes | No |
| 5b→6 monthly expansion | non-null 22,444 → 189,034 | **Expected** (×~12 months) | Yes | No |
| 6→7 signal panel | 0 | Merge/filter | N/A | Only if panel build differs |
| 7→8 complete panel | 59,157 | Merge/filter | N/A | Only if panel build differs |
| 6 vs datashare (monthly) | repo 189,034 vs DS 188,942 | **Sample + history** | Green full history | Truncated sample, not formula |

### Assessment

- **Expected loss:** stage 1→2 (14,466) from missing `ib`/`sale` or zero denominators; stage 2→3 removes **0** here because first-year rows are already NaN from missing lags.
- **CCM inner join** (stage 4→5) drops 6,808.
- **Monthly coverage matches datashare:** expanded **189,034** vs datashare **188,942** (−0.05%). No meaningful coverage gap for `chpmia` at the correct comparison stage.
- **Not more restrictive than Green**; earlier annual-vs-monthly comparison was misleading.

## `pchcapx_ia` pipeline trace

| Stage | Rows | Non-null | Notes |
| --- | ---: | ---: | --- |
| 1_raw_compustat | 43,718 | 43,718 | WRDS `comp.funda` after `ANNUAL_COMPUSTAT_WHERE` + sample date filter |
| 2_base_characteristic | 43,718 | 29,314 | Formula applied; lags created; **before** Green history mask |
| 3_lag_history_mask | 43,718 | 29,314 | After Green `count` rules on base variable |
| 4_industry_adjustment | 43,718 | 29,314 | After SIC2×fyear mean demean; before final ia row mask |
| 5_annual_output_ccm | 30,851 | 21,884 | After CCM inner join (permno attach) |
| 5b_annual_output_written | 21,884 | 21,884 | Non-null only (same as `write_character`) |
| 6_monthly_expansion | 183,244 | 183,244 | June convention; dedupe permno×signal_yyyymm |
| 7_signal_panel | 392,674 | 183,244 | On-disk signal panel merge |
| 8_complete_panel | 278,522 | 133,186 | On-disk complete panel merge |
| on_disk_annual_csv | 21,884 | 21,884 | `outputs/characteristics/individual/pchcapx_ia.csv` |
| datashare.csv (monthly, window) | 286,695 | 180,668 | Full-history GKX benchmark |
| production_check (5b recompute) | 21,884 | 21,884 | Must match 5b |

### Where observations are lost

| Transition | Rows lost (non-null) | Expected? | Green parity? | Repo more restrictive? |
| --- | ---: | --- | --- | --- |
| 1→2 base formula / divide-by-zero | 14,404 | Partial (missing inputs) | Yes | No |
| 2→3 lag-history mask | 0 | **Yes** (Green count rules) | Yes | No |
| 3→4 industry demean | 0 | No (same non-null) | Yes | No |
| 4→5 CCM inner join | 7,430 | **Yes** (no CRSP link) | Yes | No |
| 5→5b NaN/inf drop | 0 | **Yes** (`write_character`) | Yes | No |
| 5b→6 monthly expansion | non-null 21,884 → 183,244 | **Expected** (×~12 months) | Yes | No |
| 6→7 signal panel | 0 | Merge/filter | N/A | Only if panel build differs |
| 7→8 complete panel | 50,058 | Merge/filter | N/A | Only if panel build differs |
| 6 vs datashare (monthly) | repo 183,244 vs DS 180,668 | **Sample + history** | Green full history | Truncated sample, not formula |

### Assessment

- **Expected loss:** stage 2→3 (first fiscal year) plus capx denominator zeros → NaN.
- **Capx imputation** (`ppent-lag(ppent)` when missing) matches Green `count>=2`.
- **CCM + truncated sample** explain datashare monthly gap; not a formula bug.

## Root-cause summary

| Cause | Affects coverage? | Implementation bug? |
| --- | --- | --- |
| Annual CSV vs monthly datashare counting | **Yes** (misleading) | No |
| WRDS sample 2018–2023 vs full GKX history | **Yes** (major) | No |
| Green lag rules (`count<3` chato, `count=1` chpm/pchcapx) | **Yes** (expected) | No |
| CCM inner join (no permno → drop) | **Yes** (major) | No |
| `write_character` drops NaN/inf | **Yes** (expected) | No |
| Industry demean before vs after row mask | Levels only | No (ordering only) |
| Formula / SIC2 / mean demean | No extra loss | **No bug found** |

## Conclusion

The apparent coverage gap (e.g. `chatoia` **14,184** annual CSV vs **190,718** datashare monthly) is **not** evidence of an implementation bug. It is primarily:

1. **Apples-to-oranges counting** — annual fiscal CSV rows vs monthly June-expanded datashare.
2. **Stale on-disk `chatoia.csv`** — 14,184 rows (fiscal dates from 2019-06-30 only); fresh WRDS recompute yields **18,126** annual / **136,133** monthly non-null.
3. **Truncated-sample builds** — remaining `chatoia` monthly gap vs datashare (−54,585) reflects missing pre-2018 fiscal history in the 2018–2023 WRDS pull; `chpmia` and `pchcapx_ia` monthly counts **match datashare** at the correct stage.
4. **Expected Green lag requirements** — `chato` needs two prior fiscal years; in the truncated window this loss appears at stage 1→2 (missing lags), not a separate mask step.

| Variable | Root cause of low agreement | Implementation bug? |
| --- | --- | --- |
| `chatoia` | Truncated lag history + stale CSV + annual/monthly mismatch | **No** |
| `chpmia` | Mostly annual-vs-monthly confusion; levels differ from sample industry means | **No** |
| `pchcapx_ia` | Same as `chpmia`; monthly counts align with datashare | **No** |

**Do not implement additional GKX predictors** until a full-history WRDS rebuild confirms datashare level correlations for `chatoia`. No formula change recommended from this audit.

---

## Post-rebuild update (2026-06-14)

`chatoia.csv` rebuilt from **full Compustat history** (`STOCK_CHARACTERS_SAMPLE_*` unset). **Formula unchanged.**

| Metric | Stale sample build | Full-history rebuild |
| --- | ---: | ---: |
| Annual rows | 14,184 | **213,837** |
| `datadate` min | 2019-06-30 | **1976-12-31** |
| Monthly non-null (201801–202312) | 136,133 | **282,410** |
| Datashare monthly non-null | 190,718 | 190,718 |
| Paired rows vs datashare | 21,070 | **169,375** |

**Coverage gap resolved** at the correct monthly comparison stage. Signal and complete panels rebuilt. `chatoia` level Pearson/Spearman remain near zero — a separate issue from coverage; no formula change made.
